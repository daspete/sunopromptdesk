{
  "name": "musicpromptmaker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"nodemon -L --watch server --watch shared --ext ts --exec node server/index.ts\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.ts"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/compression": "^1.8.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.14",
    "typescript": "^5.6.0",
    "vite": "^8.2.2"
  }
}
