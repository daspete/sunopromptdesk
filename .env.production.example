# Production config — copy to .env.production (gitignored).
# Used by scripts/publish-image.sh and docker-compose.yml (build args are baked into the bundle).
VITE_SITE_URL=https://promptdesk.example.com
VITE_PLAUSIBLE_DOMAIN=promptdesk.example.com
# VITE_PLAUSIBLE_SRC=https://plausible.example.com/js/script.js
# host port for docker compose
PORT=3000
